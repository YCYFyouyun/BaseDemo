<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.template.demo">

    <application
        android:name=".app.comm.AppApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup,android:name">
        <activity android:name=".app.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".app.account.login.LoginActivity"
                  android:launchMode="singleTask"/>
        <activity android:name=".app.shop.ShopActivity"/>
        <activity android:name=".app.web.WebActivity"/>
        <!-- ...... -->
    </application>

</manifest>